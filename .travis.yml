sudo: false
language: cpp
compiler: clang
script: 
  - make
  - ./wip-nameless-gaem
sources:
  - llvm-toolchain-precise-3.8
addons:
  apt:
    packages:
    - clang-3.8
    - libsfml-dev
    - libbox2d-dev
